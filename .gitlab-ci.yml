integration:
  script:
    - nosetests nagare/services/tests/dependencies_test.py

production:
  type: deploy
  script:
    - python setup.py sdist
    - pip install twine
    - twine upload dist/*
  only:
    - release


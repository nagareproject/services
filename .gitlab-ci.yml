integration:
  script:
    - pip install nose
    - setests nagare/services/tests/dependencies_test.py nagare/services/tests/plugins_test.py

production:
  type: deploy
  script:
    - python setup.py sdist
    - pip install twine
    - twine upload dist/*
  only:
    - release


integration:
  script:
    - pip install nose
    - python setup.py test

production:
  type: deploy
  script:
    - python setup.py sdist
    - pip install twine
    - twine upload dist/*
  only:
    - release

